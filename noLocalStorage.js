// All of these functions are "do nothing" versions of
// the functions used to interact with localStorage if
// the browser supported that feature.
function getTodoItems() {
    console.log("Error: you don't have localStorage!");
}

function saveTodoItem(todoItem) {
    console.log("Error: you don't have localStorage!");
}

function removeTodoItem(id) {
    console.log("Error: you don't have localStorage!");
}